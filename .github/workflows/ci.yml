name: ci

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # TODO: install Godot 4.5 CLI (pin version) and cache it.

      - name: Run smoke
        run: ./tools/ci/run-smoke.sh

      - name: Run tests
        run: ./tools/ci/run-tests.sh
