# opencode configuration

This folder contains:
- `agent/`: subagent definitions
- `command/`: high-level commands for the agent to run
- `skill/`: reusable implementation playbooks
- `tool/`: custom tools (e.g., image generation)
- `mcp/`: recommended MCP server wiring

Keep these aligned with `spec.md`.
