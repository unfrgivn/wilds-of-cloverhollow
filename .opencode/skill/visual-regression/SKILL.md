# Skill: Visual regression

Goal:
Diff deterministic capture frames against baselines.

Must:
- store baselines under baselines/visual/<scenario_id>/frames
- generate diffs + HTML report under reports/visual-diff/...
- require intentional baseline update step
