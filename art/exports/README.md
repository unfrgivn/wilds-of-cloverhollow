# Exports (Game-ready)

Place cropped/normalized PNGs that are ready to import into Godot here.

Conventions:
- stable filenames with `_vNN` versioning
- transparent backgrounds for sprites/props/icons
- consistent scale across asset sets
