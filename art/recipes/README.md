# Art recipes

Recipes capture deterministic generation inputs:
- model
- prompt
- seed
- size constraints
- palette

Rule:
If you change the look, update the recipe and re-run the normalization pipeline.
