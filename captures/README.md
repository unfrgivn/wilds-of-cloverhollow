# Captures

This folder holds run artifacts produced by scripted scenarios:
- trace logs (`trace.json`)
- screenshots (optional)
- movie files (preferred when deterministic)

By default, this folder is intended to be gitignored.
