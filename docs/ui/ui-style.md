# UI style (pixel)

- UI is also pixel-consistent; avoid vector/anti-aliased UI that clashes with pixel art.
- Use a small set of UI components:
  - box panel
  - list
  - portrait frame
  - HP/MP bars
  - status icon row

If a UI element needs new colors, update the global UI palette.
