extends Node

# Minimal placeholder. Replace with fade + spawn placement.

func goto(scene_path: String, spawn_id: String = "") -> void:
	push_warning("SceneRouter.goto not implemented yet: %s (%s)" % [scene_path, spawn_id])
